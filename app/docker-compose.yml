services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: whygame
      POSTGRES_PASSWORD: whygame
      POSTGRES_DB: whygame
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
